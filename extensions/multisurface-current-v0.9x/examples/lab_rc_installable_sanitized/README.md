# Sanitized Installable Runtime Example

This folder shows the activation package shape used by an external surface to connect to a remote CURRENT backend.

It intentionally contains placeholders instead of live Drive file ids, OAuth tokens, client secrets, private keys, user email addresses, or private memory.

The corresponding runtime proof reports are in:

```text
reports/runtime_proofs/
```
